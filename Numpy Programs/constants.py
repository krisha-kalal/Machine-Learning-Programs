import numpy as np 
print("pi:", np.pi) # Returns the value of π (pi).

import numpy as np 
print("e:", np.e) # Returns the value of e (Euler's number).

import numpy as np 
print("inf:", np.inf) # Represents positive infinity.

import numpy as np 
print("negative inf:", -np.inf) # Represents negative infinity.

import numpy as np 
print("nan:", np.nan) # Represents "Not a Number" (NaN).
