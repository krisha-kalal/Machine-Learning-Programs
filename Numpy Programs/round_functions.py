import numpy as np 
print("round():", np.round(0.5)) # Rounds a number to the nearest integer.

import numpy as np 
print("around():", np.around(3.5)) # Rounds a number to the nearest integer (same as `round`).

import numpy as np 
print("floor():", np.floor(9.7)) # Rounds a number down to the nearest integer.

import numpy as np 
print("ceil():", np.ceil(9.7)) # Rounds a number up to the nearest integer.

import numpy as np
print("trunc():", np.trunc(1.5)) # Truncates the decimal part of a number.
